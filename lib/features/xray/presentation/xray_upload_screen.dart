import 'package:flutter/material.dart';
class XrayUploadScreen extends StatelessWidget {
  const XrayUploadScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Xray Upload')));
}
